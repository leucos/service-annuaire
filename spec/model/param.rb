#coding: utf-8
require_relative '../helper'

# ParamUser et ParamEtablissement ont exactement la même structure
# et partage le même code
describe "ParamUser and ParamEtablissement" do
  it "doesn't accept valeur with wrong type_param" do
  end

  it "doesn't accept param_user for a parameter" do
  end

  it "doesn't accept param_etab for a preference" do
  end
end